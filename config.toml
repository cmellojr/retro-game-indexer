# retro-game-indexer configuration
#
# Edit this file to configure models and refine detection results.
# The application works normally without this file (all values are optional).

# ── Model settings ──────────────────────────────────────────────────

[whisper]
# model_size = "base"          # tiny, base, small, medium, large
# device = "cpu"               # cpu or cuda
# compute_type = "int8"        # int8, float16, float32

[gliner]
# model_name = "urchade/gliner_base"
# device = "cpu"               # cpu or cuda

# ── Pipeline calibration ────────────────────────────────────────────

[games]
# Minimum confidence score (0.0-1.0). Default: 0.7
# threshold = 0.8

# Terms to always reject (false positives you identified).
# These are added to the built-in stopwords and are case-insensitive.
blocklist = []

# Map transcription variants to the correct name.
# Stable aliases go in data/datasets/games/aliases.json.
# Use this section for quick overrides during calibration.
# Example: { "Pico Stech" = "PicoStation" }
[games.aliases]


[maintenance]
# Minimum confidence score (0.0-1.0). Default: 0.6
# threshold = 0.7

blocklist = []

[maintenance.aliases]
